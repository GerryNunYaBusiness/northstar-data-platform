services:

  integration-sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest

    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${NORTHSTAR_INTEGRATION_SQL_PASSWORD}

    ports:
      - "14330:1433"